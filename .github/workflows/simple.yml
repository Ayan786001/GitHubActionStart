name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a string
        run: echhho "Hello world"

      - name: Multiline script
        run: |
          node -v
          npm -v
